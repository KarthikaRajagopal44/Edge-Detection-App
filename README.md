# Edge-Detection-App

A simple Streamlit application that allows users to upload an image and perform Canny Edge Detection with real-time adjustable thresholds.

### Install dependencies to run this

```bash
pip install streamlit opencv-python-headless pillow numpy

### To run the code

```bash
streamlit run canny-edge-app.py
